{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (Devcontainer)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "python": "/usr/local/bin/python",
            "console": "integratedTerminal"
        }
    ]
}
